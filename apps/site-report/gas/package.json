{
  "name": "site-report-gas",
  "version": "0.1.0",
  "private": true,
  "description": "Google Apps Script backend for the site-report project (Task 1 scaffold — no business logic yet).",
  "scripts": {
    "build": "node esbuild.config.js",
    "test": "jest",
    "typecheck": "tsc --noEmit",
    "push": "npm run build && clasp push"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2",
    "@types/google-apps-script": "^1.0.83",
    "@types/jest": "^29.5.12",
    "esbuild": "^0.23.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.5.4"
  }
}
