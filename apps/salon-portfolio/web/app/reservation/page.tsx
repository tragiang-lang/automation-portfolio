import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function ReservationPage() {
  return (
    <PagePlaceholder
      title="Reservation — coming in a later phase"
      description="The reservation wizard (createReservation) is not implemented yet."
    />
  );
}
