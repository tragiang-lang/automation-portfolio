import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function ReservationCancelPage() {
  return (
    <PagePlaceholder
      title="Cancel reservation — coming in a later phase"
      description="The cancellation-request flow (requestCancellation) is not implemented yet."
    />
  );
}
