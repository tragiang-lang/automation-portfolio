# Client projects

`projects/<year>/<slug>/`, one isolated folder per client, created by `create-project`.
Projects depend on Core Assets (by version). Core Assets never depend on projects.
`2026/demo-hair-salon` is the Phase 1 end-to-end demo, and `2026/demo-restaurant` shows that a
second industry needs no core changes.
