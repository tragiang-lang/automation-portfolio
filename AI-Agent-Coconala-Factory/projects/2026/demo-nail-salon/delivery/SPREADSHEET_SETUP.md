# スプレッドシートの構成 — Demo Nail Salon（デモ）

シートは Apps Script の **setupSpreadsheet** で自動作成されます（[SETUP.md](SETUP.md)「4」）。手で作る必要はありません。
**1行目（見出し）の名前は変更しないでください。** 列の順番の入れ替えや、右側への列の追加は問題ありません。

## シートと列

### `CONFIG`

| 列 | | 内容 |
|---|---|---|
| `key` | 必須 | 設定キー（変更しないでください） |
| `value` |  | 設定値 |
| `description` |  | 説明 |

### `CUSTOMERS`

| 列 | | 内容 |
|---|---|---|
| `customerId` | 必須 | お客様ID (CUS-YYYYMMDD-XXXXXX) |
| `lineUserId` |  | LINE ユーザーID（個人情報） |
| `name` |  | お名前（個人情報） |
| `email` |  | メールアドレス（個人情報） |
| `phone` |  | 電話番号（個人情報） |
| `createdAt` | 必須 | 登録日時 (ISO 8601) |
| `updatedAt` |  | 更新日時 (ISO 8601) |

### `SERVICES`

| 列 | | 内容 |
|---|---|---|
| `serviceId` | 必須 | メニューID（英数字・重複不可 例: S001） |
| `name` | 必須 | メニュー名 |
| `category` |  | カテゴリ |
| `durationMinutes` |  | 所要時間（分） |
| `price` |  | 料金（税込・円） |
| `description` |  | 説明 |
| `active` | 必須 | 表示する (TRUE/FALSE) |
| `displayOrder` |  | 表示順（小さい順） |

### `RESERVATIONS`

| 列 | | 内容 |
|---|---|---|
| `reservationId` | 必須 | 予約番号 (RES-YYYYMMDD-XXXXXX) |
| `submissionId` | 必須 | 重複防止用ID（LINE webhookEventId 等） |
| `createdAt` | 必須 | 受付日時 (ISO 8601) |
| `updatedAt` |  | 更新日時 (ISO 8601) |
| `customerId` |  | お客様ID |
| `lineUserId` |  | LINE ユーザーID（個人情報） |
| `name` |  | お名前（個人情報） |
| `email` |  | メールアドレス（個人情報） |
| `phone` |  | 電話番号（個人情報） |
| `serviceId` |  | メニューID |
| `partySize` |  | 人数 |
| `date` | 必須 | 日付 (YYYY-MM-DD) |
| `startTime` | 必須 | 開始時刻 (HH:mm) |
| `endTime` | 必須 | 終了時刻 (HH:mm) |
| `durationMinutes` |  | 所要時間（分） |
| `status` | 必須 | 状態（REQUESTED=リクエスト / CONFIRMED=確定 / CANCELLED=キャンセル） |
| `source` |  | 受付経路 |
| `notes` |  | ご要望・メモ（個人情報） |

### `INQUIRIES`

| 列 | | 内容 |
|---|---|---|
| `inquiryId` | 必須 | 受付番号 (INQ-YYYYMMDD-XXXXXX) |
| `submissionId` | 必須 | 重複防止用ID（LINE webhookEventId 等） |
| `createdAt` | 必須 | 受付日時 (ISO 8601) |
| `customerId` |  | お客様ID |
| `lineUserId` |  | LINE ユーザーID（個人情報） |
| `name` |  | お名前（個人情報） |
| `email` |  | メールアドレス（個人情報） |
| `phone` |  | 電話番号（個人情報） |
| `message` | 必須 | お問い合わせ内容（個人情報） |
| `source` |  | 受付経路 |
| `status` | 必須 | 対応状況（NEW=未対応 / IN_PROGRESS=対応中 / DONE=完了） |

## `CONFIG` シートの設定項目

初期値はご要望の内容から入力済みです。変更は `value` 列を書き換えるだけで、すぐに反映されます。

| key | | 内容 | 初期値 |
|---|---|---|---|
| `hours.mon` |  | 営業時間 (mon) 例: 10:00-19:00 / 定休日は closed | `closed` |
| `hours.tue` |  | 営業時間 (tue) 例: 10:00-19:00 / 定休日は closed | `closed` |
| `hours.wed` |  | 営業時間 (wed) 例: 10:00-19:00 / 定休日は closed | `11:00-20:00` |
| `hours.thu` |  | 営業時間 (thu) 例: 10:00-19:00 / 定休日は closed | `11:00-20:00` |
| `hours.fri` |  | 営業時間 (fri) 例: 10:00-19:00 / 定休日は closed | `11:00-20:00` |
| `hours.sat` |  | 営業時間 (sat) 例: 10:00-19:00 / 定休日は closed | `10:00-19:00` |
| `hours.sun` |  | 営業時間 (sun) 例: 10:00-19:00 / 定休日は closed | `10:00-19:00` |
| `reservation.slotMinutes` |  | 予約枠の間隔（分） | `30` |
| `reservation.defaultDurationMinutes` |  | メニュー未指定時の所要時間（分） | `90` |
| `reservation.minLeadHours` |  | 何時間前まで予約を受け付けるか | `3` |
| `reservation.maxDaysAhead` |  | 何日先まで予約を受け付けるか | `60` |
| `reservation.capacity` |  | 同じ時間帯に受け付ける予約数（席数・スタッフ数） | `2` |
| `reservation.closedDates` |  | 臨時休業日（カンマ区切り 例: 2026-12-31,2027-01-01） | `` |
| `notification.ownerEmail` |  | 新しいお問い合わせ・予約リクエストの通知先メールアドレス（店舗用） | `owner@example.com` |
| `labels.serviceList` |  | メニュー一覧の見出し | `メニュー・料金` |
| `business.name` | 必須 | 店舗名 | `Demo Nail Salon` |
| `business.address` |  | 住所 | `東京都港区南青山0-0-0 デモビル3F` |
| `business.phone` |  | 電話番号 | `03-0000-0000` |
| `business.mapUrl` |  | 地図URL（Google マップ等） | `https://example.com/map/demo-nail-salon` |
| `business.hoursText` |  | 営業時間の案内文 例: 10:00〜19:00（火曜定休） | `水〜金 11:00-20:00／土日祝 10:00-19:00（月・火曜定休）` |
| `business.accessNote` |  | アクセス案内 例: 〇〇駅 徒歩3分 | `表参道駅から徒歩5分` |

## 個人情報の扱い

「（個人情報）」と書かれた列には、お客様の LINE ID・お名前・連絡先・お問い合わせ内容が入ります。
スプレッドシートの共有は必要な方だけにし、不要になったデータは定期的に削除してください。

---

<sub>Generated by AI-Agent-Coconala-Factory 0.1.0 for 2026/demo-nail-salon (nail_salon; reservation-basic-v1@1.0.0, service-menu-v1@1.0.0, inquiry-basic-v1@1.0.0, business-info-v1@1.0.0).</sub>
